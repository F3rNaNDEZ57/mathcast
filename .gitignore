# Manim output - renders, partial movies, TeX artifacts, TTS cache
media/

# Generated scenes are build artifacts, not source
generated/

# Per-lesson working files: outline, plan, script, config
work/

# Learning material belongs to whoever put it there, not to this repo - and this
# repo is public. The one exception is the project's own design brief, which was
# already tracked before inputs/ existed.
inputs/*
!inputs/Automating Manim Video Creation.md

# Written into a project by /mathcast:init only when the IPv6 probe fails.
# The template lives at assets/ipv4_first.py and IS tracked.
/ipv4_first.py

# Kokoro model files - 338 MB, belong in a shared cache, never in a repo
*.onnx
voices-*.bin

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Secrets
.env

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
